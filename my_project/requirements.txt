# ===========================================================================
# VGIS Django 框架依赖（已在 Django 6.0.6 + Python 3.12.10 下实测通过）
# 环境：E:\claudecode\venv_6.06
# 数据库：PostgreSQL 18.4 @ 192.168.3.40:12326 (MYDB)
# ===========================================================================

# ---- 框架核心 ----
Django==6.0.6
djangorestframework==3.18.1
# 替代已停止维护、且不兼容新版 Django 的 django-rest-swagger
drf-spectacular==0.30.0
django-filter==26.1
django-cors-headers==4.9.0
django-apscheduler==0.7.0

# ---- 数据库（PostGIS 引擎需要 GDAL/GEOS 动态库，Windows 见 settings.py 的 GDAL_LIBRARY_PATH）----
psycopg2-binary==2.9.13
djangorestframework-gis==1.3.0

# ---- 异步 / 实时 ----
celery==5.6.3
channels==4.3.2
channels_redis==4.3.0
daphne==4.2.3
django-redis==7.0.0
# 注意：Redis 服务端是 5.0.x，redis-py 6.0 起默认走 RESP3 握手（HELLO 3），
# 而 HELLO 命令 Redis 6.0 才支持，会导致缓存/channels/celery 全部连不上
# （celery 的 kombu 无法通过配置注入 protocol，只能靠客户端默认值）。
# 因此这里必须锁在 5.x。Redis 升级到 6.0+ 之后可以放开。
redis==5.2.1

# ---- 加解密 / 许可 ----
pycryptodome==3.23.0
cryptography==50.0.1
license_authorize==1.0.7

# ---- 内部库 ----
vgis-utils==1.4.9
vgis-log==1.0.9
vgis-database==1.0.17
vgis-office==1.1.8
vgis_encrption==1.0.5

# ---- 工具 / 文档处理 ----
toollib==2.2.6
Pillow==12.3.0
numpy==2.5.3
pandas==3.0.5

# ---- 部署 ----
waitress==3.0.2

# ---- gis_service 模块依赖（阶段 gis_service）----
# 已在 venv_6.06 实测可装：mapbox-vector-tile-2.2.0 / mercantile-1.2.1 / pmtiles-3.8.0
# 注意：mapbox-vector-tile 强制 protobuf<7（拉了 6.33.6）；
#       与 vgis_django_service 其它模块（如 vgis-utils）若共用 protobuf 需留意版本锁
shapely>=2.0.0
mercantile>=1.2.0
mapbox-vector-tile>=2.0.0
pmtiles>=3.0.0
rasterio>=1.3.0
numpy>=1.24.0
