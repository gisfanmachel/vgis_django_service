# ============================================================
# VGIS Django 框架仓库通用忽略规则
# 说明：my_project / tcp_server / web_socket 三个服务各自独立部署，
#       运行时会在各自目录下生成 logs/ 与 static/upload/，一律不入库。
# ============================================================

# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# ---- 虚拟环境 ----
venv/
.venv/
env/
ENV/

# ---- IDE / 编辑器 ----
.idea/
.vscode/
*.iml
*.swp
*.swo
*~

# ---- 运行时产物（三个服务通用）----
logs/
log/
*.log
*.log.*
# 上传目录（config.UPLOAD_FOLDER = static/upload）
# 注意：带中间斜杠的 pattern 会被锚定到仓库根，必须加 **/ 才能匹配到 tcp_server/、web_socket/ 下的同名目录
**/static/upload/
# Django 静态资源上传目录（settings.UPLOAD_ROOT = <APP_NAME>/<STATIC_NAME>/upload，
# STATIC_NAME 各项目不同，这里用 **/ 兜住 my_static / pnt_static 等任意命名）
**/*_static/upload/

# ---- 测试 / 类型检查缓存 ----
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# ---- 操作系统 ----
.DS_Store
Thumbs.db
desktop.ini

# ---- 本地环境变量（Stage A 配置驱动引入）----
# 真值放 .env，不入库；.env.example 提交作为文档
.env
.env.local
.env.*.local
