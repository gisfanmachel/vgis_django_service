# 运行环境说明

> 本文件记录 VGIS Django 框架及仓库内其它服务的**实际运行环境**（版本 + 路径 + 连接信息）。
> 最后核对时间：**2026-09-12**，核对方式为在本机实跑通过全量功能测试（39/39）。

---

## 一、版本总览

| 组件 | 版本 | 说明 |
|---|---|---|
| Python | **3.12.10** (CPython) | |
| Django | **6.0.6** | 要求 PostgreSQL ≥ 14 |
| PostgreSQL | **18.4** (Debian) | 原生满足 Django 6 要求，无需版本兼容垫片 |
| PostGIS | **3.6.4** | 框架使用 postgis 引擎 |
| Redis | **5.0.14.1** | Windows 版；版本较老，见「四、版本约束」 |
| Celery | **5.6.3** | broker/result 均用 Redis |
| DRF | **3.18.1** | |
| GDAL / GEOS | GDAL **3.0.4** / GEOS | 仅 Windows 需手工指定路径 |

---

## 二、路径

### 解释器（框架与依赖所在 venv）

```
E:\claudecode\venv_6.06\Scripts\python.exe
```

所有依赖都装在这个 venv 里，**不要用系统 Python**。启动、跑测试、跑 `manage.py` 一律用这个完整路径。

### 代码

| 内容 | 路径 |
|---|---|
| 仓库根 | `D:\系统开发\VGIS-DEV-LIB\Django框架\vgis_django_service` |
| Django 框架 | `…\vgis_django_service\my_project` |
| 通用 TCP 服务 | `…\vgis_django_service\tcp_server` |
| 通用 WebSocket 服务 | `…\vgis_django_service\web_socket` |
| 库升级脚本 | `…\vgis_django_service\database\MYDB_upgrade.sql` |

### 许可文件（登录接口硬依赖）

```
C:\license\django_study_license.lic
```

配置项：`settings.WINDOWS_LICENSE_PATH` / `LINUX_LICENSE_PATH`。
同目录还有其它项目的许可（cysxyd / dkwrj / dzyq / tfwrj / xjwj / ynai），**互不通用**，
用错会报 `A PEM file must be at least 3 lines` 或校验返回 False。

### GIS 原生库（仅 Windows 需要）

```
C:\Program Files\GDAL\bin\gdal300.dll
C:\Program Files\GDAL\bin\geos_c.dll
C:\Program Files\GDAL\bin\proj\
```

`settings.py` 末尾已按 `os.name == 'nt'` 自动探测并设置
`GDAL_LIBRARY_PATH` / `GEOS_LIBRARY_PATH` / `PROJ_LIB`。
Linux 部署时该段自动跳过（服务器上一般在 ld 路径里）。

---

## 三、服务端连接信息

### 数据库

| 项 | 值 |
|---|---|
| 地址 | `192.168.3.40:12326` |
| 库名 | `MYDB` |
| 账号 / 密码 | `postgres` / `postgres` |
| 引擎 | `django.contrib.gis.db.backends.postgis` |
| 连接池 | `OPTIONS.connect_timeout = 10` |

> 注意端口是 **12326**，不是 PostgreSQL 默认的 5432。

### Redis

| 项 | 值 |
|---|---|
| 地址 | `192.168.3.80:6379` |
| db0 | Django 业务缓存 |
| db1 | channels 通道层 |
| db2 | celery broker + result |

### Celery

```
broker  : redis://192.168.3.80:6379/2
result  : redis://192.168.3.80:6379/2
timezone: Asia/Shanghai
```

---

## 四、版本约束（重要，改版本前先看这里）

### 1. redis-py 必须锁在 5.x

服务端 Redis 是 **5.0.14**，而 **redis-py 6.0 起默认走 RESP3 握手**（连接时发 `HELLO 3`），
`HELLO` 命令 Redis 6.0 才支持 → 报 `unknown command 'HELLO'`。

各层表现与对策：

| 层 | 现象 | 对策 |
|---|---|---|
| Django 缓存 | 因配了 `IGNORE_EXCEPTIONS: True`，**失败被静默吞掉**，`cache.get()` 只返回 None 不报错 | OPTIONS 里加 `protocol: 2` |
| channels 通道层 | 连接失败 | hosts 里加 `protocol: 2` |
| Celery | 连不上 broker | **kombu 5.6.2 的 `_connparams()` 是硬编码字典，没有任何注入 protocol 的入口**（URL 传 `?protocol=2` 报 `unexpected keyword`），配置层无解 → 只能靠客户端默认值，故 `requirements.txt` 锁 `redis==5.2.1` |

**Redis 升级到 6.0+ 之后，可以去掉这三处 protocol 配置并放开 redis-py 版本锁定。**

### 2. PostgreSQL 必须 ≥ 14

Django 6.0 的 postgres 后端声明 `minimum_database_version = (14,)`，
在建立连接时检查，低于该版本直接抛 `NotSupportedError`。
当前 18.4 原生满足。若迁到 PG 12/13 的库会连不上。

### 3. 已从旧版本迁移、不能再改回去的点

| 旧写法 | 现状 | 原因 |
|---|---|---|
| `rest_framework_swagger` | 已换 **drf-spectacular**（`/docs/` + `/docs/schema/`） | 该项目已停止维护且不兼容新版 Django |
| `rest_framework.documentation.include_docs_urls` | 已移除 | DRF 3.18 已删除该模块 |
| `django.db.backends.postgresql_psycopg2` | 只能写 `postgresql`（本项目用 `postgis`） | Django 4.0 起已删除该别名 |
| `USE_L10N = True` | 已删除 | Django 5.0 已移除该配置 |

---

## 五、启动方式

### Django 框架

```bash
cd D:\系统开发\VGIS-DEV-LIB\Django框架\vgis_django_service\my_project

# 自检（不需要数据库连接）
E:\claudecode\venv_6.06\Scripts\python.exe manage.py check

# 开发启动
E:\claudecode\venv_6.06\Scripts\python.exe manage.py runserver 0.0.0.0:17276

# 生产启动（waitress）
E:\claudecode\venv_6.06\Scripts\python.exe run.py
```

API 前缀 `/my_api/`，接口文档 `/docs/`。

### Celery worker

```bash
E:\claudecode\venv_6.06\Scripts\python.exe -m celery -A my_project worker -l info
```

### 两个独立服务（通用 TCP / WebSocket）

各自带 `requirements.txt`，无第三方私有依赖，**可用任意 Python 3.10+**，
通过环境变量注入配置（详见各自目录的 `readme.md`）：

```bash
cd …\vgis_django_service\tcp_server && python main.py
cd …\vgis_django_service\web_socket && python main.py
```

---

## 六、关键依赖清单

完整清单见 `my_project/requirements.txt`（已按实测环境对齐）。要点：

```
Django==6.0.6
djangorestframework==3.18.1
drf-spectacular==0.30.0
djangorestframework-gis==1.3.0
psycopg2-binary==2.9.13
celery==5.6.3
channels==4.3.2  channels_redis==4.3.0  daphne==4.2.3
django-redis==7.0.0
redis==5.2.1              # 见「四、版本约束」，不可随意升级
license_authorize==1.0.7
vgis-utils / vgis-log / vgis-database / vgis-office / vgis_encrption
```

---

## 七、本环境下的验证结论

- `manage.py check`：**0 issues**
- 27 个模型与数据库表**一一对应，无缺表**
- 通用功能冒烟测试：**39/39 通过**
  （路由与文档 3 · 免鉴权接口 3 · 系统管理列表 13 · 自定义 action 10 · 数据字典完整 CRUD ·
   单/多文件上传 · 忘记密码全链路 · 登录/Token/鉴权 6）
- Redis 缓存、channels 通道层、Celery worker 启动与任务注册均实测通过

### 测试账号

| 账号 | 密码 | 备注 |
|---|---|---|
| `admin` | `Test@12345` | 密码由忘记密码功能的测试重置过 |
| `chenxw` | `admin` | |
