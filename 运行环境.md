# 运行环境说明

> 本文件记录 VGIS Django 框架及仓库内其它服务的**实际运行环境**（版本 + 路径 + 连接信息）。
> 最后核对时间：**2026-09-12**，核对方式为在本机实跑通过全量功能测试（39/39）。

---

## 一、版本总览

| 组件 | 版本 | 说明 |
|---|---|---|
| Python | **3.12.14** (CPython, MSC v.1944, conda-forge) | venv 于 2026-09-14 重建升级 |
| Django | **6.0.6** | 要求 PostgreSQL ≥ 14 |
| PostgreSQL | **18.4** (Debian) | 原生满足 Django 6 要求，无需版本兼容垫片 |
| PostGIS | **3.6.4** | 框架使用 postgis 引擎 |
| Redis | **5.0.14.1** | Windows 版；版本较老，见「四、版本约束」 |
| Celery | **5.6.3** | broker/result 均用 Redis |
| DRF | **3.18.1** | |
| GDAL / GEOS | GDAL **3.13.3** / GEOS（随 venv） | 仅 Windows 需手工指定路径，见下 |
| osgeo Python 绑定 | **3.13.3**（194 个驱动可用） | 在 `venv\Lib\site-packages\osgeo\` |

---

## 二、路径

### 解释器（框架与依赖所在 venv）

```
E:\claudecode\venv_6.06\Scripts\python.exe
```

所有依赖都装在这个 venv 里，**不要用系统 Python**。启动、跑测试、跑 `manage.py` 一律用这个完整路径。

### 代码

| 内容 | 路径 |
|---|---|
| 仓库根 | `D:\系统开发\VGIS-DEV-LIB\Django框架\vgis_django_service` |
| Django 框架 | `…\vgis_django_service\my_project` |
| 通用 TCP 服务 | `…\vgis_django_service\tcp_server` |
| 通用 WebSocket 服务 | `…\vgis_django_service\web_socket` |
| 库升级脚本 | `…\vgis_django_service\database\MYDB_upgrade.sql` |

### 许可文件（登录接口硬依赖）

```
C:\license\django_study_license.lic
```

配置项：`settings.WINDOWS_LICENSE_PATH` / `LINUX_LICENSE_PATH`。
同目录还有其它项目的许可（cysxyd / dkwrj / dzyq / tfwrj / xjwj / ynai），**互不通用**，
用错会报 `A PEM file must be at least 3 lines` 或校验返回 False。

### GIS 原生库（仅 Windows 需要）

**用与 venv 配套的 GDAL 3.13.3**（不是 `C:\Program Files\GDAL` 那份 3.0.4）：

```
E:\claudecode\_tools\gdal-3.13.3\Library\bin\gdal.dll        ← 主库，名字是裸的 gdal.dll
E:\claudecode\_tools\gdal-3.13.3\Library\bin\geos_c.dll
E:\claudecode\_tools\gdal-3.13.3\Library\share\proj\         ← proj.db
E:\claudecode\_tools\gdal-3.13.3\Library\share\gdal\         ← GDAL_DATA
E:\claudecode\_tools\gdal-3.13.3\Library\lib\gdalplugins\    ← GDAL_DRIVER_PATH
```

`settings.py` 末尾（`os.name == 'nt'` 时）会按候选列表自动探测，**优先级**：

1. 环境变量 `GDAL_BIN_DIR`（换机器/换版本时用这个覆盖）
2. `E:\claudecode\_tools\gdal-3.13.3\Library\bin` ← 与 venv 配套
3. `C:\Program Files\GDAL\bin`（旧的 3.0.x 独立安装，仅作兜底）
4. `C:\OSGeo4W64\bin` / `C:\OSGeo4W\bin`

探测到之后会设置 `GDAL_LIBRARY_PATH` / `GEOS_LIBRARY_PATH`，并**覆盖**（不是 setdefault）
`PROJ_LIB` / `GDAL_DATA` / `GDAL_DRIVER_PATH`，同时把该目录置于 `PATH` 最前。
Linux 部署时该段自动跳过（服务器上一般在 ld 路径里）。

#### 选到非配套 GDAL 时会打警告

如果最终选中的不是上面第 2 项（配套的那份），启动时会输出一条 `RuntimeWarning`：

```
[VGIS] 当前选用的 GDAL 不是与 venv 配套的那份，osgeo 绑定可能不可用。
       实际选用: ...       配套路径: ...
       影响: venv 里 osgeo 的 _gdal.pyd 依赖【同目录】的 gdal.dll ...
       处理: 确认上面的配套路径存在；或把它设到环境变量 GDAL_BIN_DIR 显式指定。
```

**为什么需要这条警告**：`C:\Program Files\GDAL`（3.0.4）只是兜底。一旦配套目录被删或改名，
会静默回落到它——Django 侧照样能启动、`GDAL_VERSION` 也照报，但 venv 的 `osgeo`
会报 `DLL load failed while importing _gdal`，而报错完全指不到"GDAL 选错了"。
有这条警告就能一眼定位。

#### 三个必须覆盖、不能沿用系统值的坑

本机系统级环境里装过 GeoServer / GISInternals，已经存在这些变量：

```
PROJ_LIB         = C:\Program Files\gdal\bin\proj6\share        (2019 年的 proj.db)
GDAL_DATA        = C:\Program Files\gdal\bin\gdal-data
GDAL_DRIVER_PATH = C:\Program Files\gdal\bin\gdal\plugins       (为 3.0.4 编译的插件)
```

它们属于**旧的 GDAL 3.0.4**，如果沿用会导致：

- 新版 GDAL 读到旧 proj.db → 报
  `proj.db lacks DATABASE.LAYOUT.VERSION.MAJOR / ... It comes from another PROJ installation`
  → 投影转换直接失败（`GDALException: OGR failure`）
- 加载到为旧版编译的驱动插件

所以 settings 里对这五个变量一律**直接赋值覆盖**。**不要改成 `setdefault`。**

### osgeo Python 绑定

venv 里自带 `osgeo`（GDAL 3.13.3，194 个驱动），但它的 `_gdal.pyd` 依赖**同目录**的
`gdal.dll`，因此**必须先让 settings 里的 `add_dll_directory` 执行过**才能 import：

```python
import os, django
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'my_project.settings')
django.setup()                     # ← 这一步之后 osgeo 才可用

from django.contrib.gis.gdal import GDAL_VERSION   # (3, 13, 3)
from osgeo import gdal, osr                        # 3.13.3
```

直接 `python -c "from osgeo import gdal"`（不加载 settings）会报
`ImportError: DLL load failed while importing _gdal` / `No module named '_gdal'`。
独立脚本要自己先 `os.add_dll_directory(r'E:\claudecode\_tools\gdal-3.13.3\Library\bin')`。

---

## 三、服务端连接信息

### 数据库

| 项 | 值 |
|---|---|
| 地址 | `192.168.3.40:12326` |
| 库名 | `MYDB` |
| 账号 / 密码 | `postgres` / `postgres` |
| 引擎 | `django.contrib.gis.db.backends.postgis` |
| 连接池 | `OPTIONS.connect_timeout = 10` |

> 注意端口是 **12326**，不是 PostgreSQL 默认的 5432。

### Redis

| 项 | 值 |
|---|---|
| 地址 | `192.168.3.80:6379` |
| db0 | Django 业务缓存 |
| db1 | channels 通道层 |
| db2 | celery broker + result |

### Celery

```
broker  : redis://192.168.3.80:6379/2
result  : redis://192.168.3.80:6379/2
timezone: Asia/Shanghai
```

---

## 四、版本约束（重要，改版本前先看这里）

### 1. redis-py 必须锁在 5.x

服务端 Redis 是 **5.0.14**，而 **redis-py 6.0 起默认走 RESP3 握手**（连接时发 `HELLO 3`），
`HELLO` 命令 Redis 6.0 才支持 → 报 `unknown command 'HELLO'`。

各层表现与对策：

| 层 | 现象 | 对策 |
|---|---|---|
| Django 缓存 | 因配了 `IGNORE_EXCEPTIONS: True`，**失败被静默吞掉**，`cache.get()` 只返回 None 不报错 | OPTIONS 里加 `protocol: 2` |
| channels 通道层 | 连接失败 | hosts 里加 `protocol: 2` |
| Celery | 连不上 broker | **kombu 5.6.2 的 `_connparams()` 是硬编码字典，没有任何注入 protocol 的入口**（URL 传 `?protocol=2` 报 `unexpected keyword`），配置层无解 → 只能靠客户端默认值，故 `requirements.txt` 锁 `redis==5.2.1` |

**Redis 升级到 6.0+ 之后，可以去掉这三处 protocol 配置并放开 redis-py 版本锁定。**

> ⚠️ **venv 每重建一次都要复查这条。** 2026-09-14 venv 重建后 redis-py 被升回 8.1.0，
> 症状是裸客户端报 `unknown command 'HELLO'`、celery 连不上（cache 因为配置里写了
> `protocol: 2` 侥幸还能用，容易漏判）。同时那次重建还漏装了 `celery`。
> 重建后请执行：
> ```bash
> E:\claudecode\venv_6.06\Scripts\python.exe -m pip install -r my_project/requirements.txt
> ```
> 再用「七、验证结论」里的方式把 Redis 四层（裸客户端 / cache / channels / celery）各测一遍。

### 2. PostgreSQL 必须 ≥ 14

Django 6.0 的 postgres 后端声明 `minimum_database_version = (14,)`，
在建立连接时检查，低于该版本直接抛 `NotSupportedError`。
当前 18.4 原生满足。若迁到 PG 12/13 的库会连不上。

### 3. 已从旧版本迁移、不能再改回去的点

| 旧写法 | 现状 | 原因 |
|---|---|---|
| `rest_framework_swagger` | 已换 **drf-spectacular**（`/docs/` + `/docs/schema/`） | 该项目已停止维护且不兼容新版 Django |
| `rest_framework.documentation.include_docs_urls` | 已移除 | DRF 3.18 已删除该模块 |
| `django.db.backends.postgresql_psycopg2` | 只能写 `postgresql`（本项目用 `postgis`） | Django 4.0 起已删除该别名 |
| `USE_L10N = True` | 已删除 | Django 5.0 已移除该配置 |

---

## 五、启动方式

### Django 框架

```bash
cd D:\系统开发\VGIS-DEV-LIB\Django框架\vgis_django_service\my_project

# 自检（不需要数据库连接）
E:\claudecode\venv_6.06\Scripts\python.exe manage.py check

# 开发启动
E:\claudecode\venv_6.06\Scripts\python.exe manage.py runserver 0.0.0.0:17276

# 生产启动（waitress）
E:\claudecode\venv_6.06\Scripts\python.exe run.py
```

API 前缀 `/my_api/`，接口文档 `/docs/`。

### Celery worker

```bash
E:\claudecode\venv_6.06\Scripts\python.exe -m celery -A my_project worker -l info
```

### 两个独立服务（通用 TCP / WebSocket）

各自带 `requirements.txt`，无第三方私有依赖，**可用任意 Python 3.10+**，
通过环境变量注入配置（详见各自目录的 `readme.md`）：

```bash
cd …\vgis_django_service\tcp_server && python main.py
cd …\vgis_django_service\web_socket && python main.py
```

---

## 六、关键依赖清单

完整清单见 `my_project/requirements.txt`（已按实测环境对齐）。要点：

```
Django==6.0.6
djangorestframework==3.18.1
drf-spectacular==0.30.0
djangorestframework-gis==1.3.0
psycopg2-binary==2.9.13
celery==5.6.3
channels==4.3.2  channels_redis==4.3.0  daphne==4.2.3
django-redis==7.0.0
redis==5.2.1              # 见「四、版本约束」，不可随意升级
license_authorize==1.0.7
vgis-utils / vgis-log / vgis-database / vgis-office / vgis_encrption
```

---

## 七、本环境下的验证结论

- `manage.py check`：**0 issues**
- 27 个模型与数据库表**一一对应，无缺表**
- 通用功能冒烟测试：**39/39 通过**
  （路由与文档 3 · 免鉴权接口 3 · 系统管理列表 13 · 自定义 action 10 · 数据字典完整 CRUD ·
   单/多文件上传 · 忘记密码全链路 · 登录/Token/鉴权 6）
- Redis 缓存、channels 通道层、Celery worker 启动与任务注册均实测通过

### 测试账号

| 账号 | 密码 | 备注 |
|---|---|---|
| `admin` | `Test@12345` | 密码由忘记密码功能的测试重置过 |
| `chenxw` | `admin` | |

---

## 八、Stage A-G 环境变量与可观测性（2026-09-15 v2）

本节是 SpringBoot 对齐方案 Stage A-G 的实施说明（commit `8ac3713`~`ceb1ee7`）。完整改动清单见 `CHANGELOG_2026-09-15-v2.md`。

### 8.1 配置中心（config.py）

`my_project/my_project/config.py` 集中所有可调参数，复用 `tcp_server/config.py` 的 `_env` 模式。每个变量都可用同名环境变量覆盖，详见 `my_project/.env.example`。

**默认行为零回归**：未设置任何环境变量时与 v1 完全一致（DB=192.168.3.40/mydb_test、Redis=192.168.3.80/6379、ES=enabled）。

### 8.2 DB 连接硬化 + 读副本路由

`_build_db_options()` 把 libpq 全套保活打开：

```python
{
    "keepalives": 1,
    "keepalives_idle": 30,
    "keepalives_interval": 10,
    "keepalives_count": 5,
    "options": "-c statement_timeout=30s -c idle_in_transaction_session_timeout=60s",
}
```

启用读副本：把 `DB_REPLICA_ENABLED=true` 并提供 readonly_user，`db_router.PrimaryReplicaRouter` 自动把读流量切到 replica，写仍走 default。验证：`python manage.py db_health`。

启用 pgbouncer：把 `PGBOUNCER_ENABLED=true` 并配置 `PGBOUNCER_HOST/PORT`，所有 alias 改走 pgbouncer；建议同步把 `DB_CONN_MAX_AGE=0`（pgbouncer 自己管池）。

### 8.3 缓存层（11 张热表）

`my_app/utils/cacheHelper.TableCacheHelper` + `cacheInvalidate.invalidate_table(table_name)`：

- 8 个 ViewSet 的 create/update/destroy 成功路径加了 `invalidate_table(...)`（见 sys_manage/views.py）
- Celery worker 启动 5s 后自动跑 `warm_all_caches` 任务（celery.py:worker_ready → cacheWarmer.py）
- 预热 5 张表：sys_dict_catelog / sys_dict / sys_menu / tm_district / tm_region

### 8.4 SQL 注入防护

`my_app/utils/safeSQL.py`：

- `ALLOWED_TABLES`（30+ 张业务表）
- `ALLOWED_DIRECTIONS = {asc, desc, ASC, DESC}`
- `assert_table_allowed / safe_format_table / safe_order_by / safe_ident / UnsafeIdentifierError`

启动期 AST 静态扫描：`my_app/checks.py:check_raw_sql_format_usage`（my_app.W002）。当前命中 1 处 `demo/manager.py:201`（已用 UPDATE_FIELD_WHITELIST 防御，未消除告警）。

### 8.5 Docker 化

| 文件 | 用途 |
|---|---|
| `my_project/Dockerfile` | python:3.12-slim + libpq/libgdal/libgeos/libproj + gunicorn 23.0.0 |
| `deploy/docker-compose.yml` | web + celery + nginx + redis（postgis/pgbouncer 默认注释关闭） |
| `deploy/nginx.conf` | least_conn 上游，1 worker 起步可扩到 4 |
| `deploy/pgbouncer/*` | transaction 池模式 / 10000 client / 20 pool |
| `deploy/deploy_to_40.py` | paramiko 一键部署到 192.168.3.40 |

构建：`cd my_project && docker build -t vgis-django:stage-e .`
启动：`cd deploy && docker-compose up -d --scale web=4`

### 8.6 db_health 管理命令

```bash
python manage.py db_health                # 所有 alias
python manage.py db_health --alias default # 单个
```

输出：`[default] PG PostgreSQL 18.4 ...` + `connections: active=N idle=N` + `SELECT 1 OK`

### 8.7 50 并发压测基线（stage G 验证）

```
tools/loadtest.py --users 50 --rounds 5 --port 10846
1750 req / 12.4s / 141 req/s / 零 5xx
authUser_list            p95=415ms
authUser_sqlsearch       p95=380ms
sysDept_sqlsearch        p95=1060ms（首次连接刷新造成）
sysRole_sqlsearch        p95=514ms
sysMenu_list             p95=492ms
sysParam_list            p95=488ms
sysLog_sqlsearch         p95=457ms
```

### 8.8 升级 Django 进程

改完 settings.py 必须重启 Django 进程才生效（老进程仍用旧配置）。重启方式：

```bash
# 1) TaskStop 旧 waitress 任务
# 2) 后台跑：
cd D:/系统开发/VGIS-DEV-LIB/Django框架/vgis_django_service/my_project
E:/claudecode/venv_6.06/Scripts/python.exe run.py 10846
```
